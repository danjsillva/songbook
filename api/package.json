{
  "name": "@songbook/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute songbook-db --remote --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute songbook-db --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^4.54.0"
  },
  "dependencies": {
    "@songbook/shared": "1.0.0",
    "nanoid": "^5.0.9"
  }
}
